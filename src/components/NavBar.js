// Navigation bar component to display the title of the Albums List App
function NavBar() {
    return (
        <nav className="navbar">
            <a href="/">Albums List App</a>
        </nav>
    );
}

// Export the NavBar component
export default NavBar;
